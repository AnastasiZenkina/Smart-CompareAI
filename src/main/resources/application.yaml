server:
  port: 8080

spring:
  application:
    name: smart-compare
  ai:
    openai:
      api-key: YOUR_API_KEY_HERE
      chat:
        options:
          model: gpt-4o-mini
          temperature: 0.7

logging:
  level:
    com.medicalmarcetplace.smart_compare: DEBUG
    org.springframework.ai: INFO